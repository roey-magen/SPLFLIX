#include "Watchable.h"
using namespace std;//check if legal

//Watchable::Watchable(long id, int length, const std::vector<std::string> &tags):id(id),length(length),tags(tags) {}
//
//Movie::Movie(long id, const std::string& name, int length, const std::vector<std::string>& tags) {}//constructor.
//virtual std::string toString() const;
//virtual Watchable* getNextWatchable(Session&) const;//get the next recommondation according to the user algorithm. (return null if not exist)

